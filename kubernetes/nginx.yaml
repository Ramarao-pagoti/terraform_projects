apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodeport-nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nodeport-nginx
  template:
    metadata:
      labels:
        app: nodeport-nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80